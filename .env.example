# =============================================================================
# n8n Configuration Template
# =============================================================================
#
# This file serves as a template for setting up n8n integration.
#
# SETUP INSTRUCTIONS:
# 1. Run: ./scripts/setup-n8n.sh
# 2. The script will automatically generate .env.local with actual values
# 3. DO NOT manually copy this file - let the script handle configuration
#
# The setup script will:
# - Generate secure encryption keys
# - Prompt for owner account credentials
# - Start n8n in Docker
# - Create an API key for Next.js integration
# - Configure all necessary environment variables
# =============================================================================

# -----------------------------------------------------------------------------
# n8n Core Configuration
# -----------------------------------------------------------------------------

# Encryption key for n8n credentials storage
# AUTO-GENERATED by setup-n8n.sh using openssl
N8N_ENCRYPTION_KEY=your-base64-encoded-encryption-key-here

# Timezone for n8n instance (used by n8n Docker container)
# Default: America/New_York
GENERIC_TIMEZONE=America/New_York

# n8n connection settings
# These define how Next.js connects to your n8n instance
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http

# -----------------------------------------------------------------------------
# n8n UI Settings
# -----------------------------------------------------------------------------

# Disable personalization/welcome modal on first login
N8N_PERSONALIZATION_ENABLED=false

# Disable diagnostics/telemetry
N8N_DIAGNOSTICS_ENABLED=false

# -----------------------------------------------------------------------------
# n8n Owner Account
# -----------------------------------------------------------------------------
# These credentials are used for initial setup and API key creation
# The setup script will prompt you for these values (press Enter to use defaults)
#
# Default credentials:
# Email: admin@example.com
# Password: Admin123
#
# Custom password requirements:
# - Minimum 8 characters
# - At least 1 uppercase letter
# - At least 1 number

N8N_OWNER_EMAIL=admin@example.com
N8N_OWNER_FIRST_NAME=Admin
N8N_OWNER_LAST_NAME=User
N8N_OWNER_PASSWORD=Admin123

# -----------------------------------------------------------------------------
# n8n API Integration (Next.js)
# -----------------------------------------------------------------------------

# API key for authenticating Next.js API routes with n8n
# AUTO-GENERATED by setup-n8n.sh after owner account creation
N8N_API_KEY=your-generated-api-key-here

# API request timeout in milliseconds
# Default: 10000 (10 seconds)
N8N_API_TIMEOUT=10000

# -----------------------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------------------

# Enable/disable the n8n status badge component in the UI
# Set to "true" to show connection status in your Next.js app
NEXT_PUBLIC_N8N_STATUS_BADGE_ENABLED=true
SIMPLE_FIN=
SIMPLE_FIN_ACCESS_URL=
